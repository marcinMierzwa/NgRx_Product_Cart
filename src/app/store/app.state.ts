// src/app/store/app.state.ts

// Ten interfejs będzie opisywał globalny kształt całego stanu Twojej aplikacji.
// Będzie on rozszerzany w miarę dodawania stanów dla kolejnych feature'ów.
export interface AppState {
  // np. cart: CartState;
  // Na razie zostawiamy pusty.
}